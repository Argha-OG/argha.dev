export const blogPosts = [
    {
        id: "google-antigravity",
        title: "Google Antigravity: Revolutionizing AI-Powered Development",
        description: "Explore how Google's Antigravity AI is transforming the way developers build applications with intelligent code generation and advanced reasoning.",
        date: "Nov 24, 2024",
        readTime: "12 min read",
        image: "https://images.unsplash.com/photo-1677442136019-21780ecad995?q=80&w=2070&auto=format&fit=crop",
        category: "AI & Development",
        author: "Argha Biswas",
        content: `
# Google Antigravity: Revolutionizing AI-Powered Development

Google Antigravity represents a groundbreaking leap in AI-assisted development, combining advanced language models with intelligent code generation capabilities. This powerful tool is reshaping how developers approach software engineering.

## What is Google Antigravity?

Google Antigravity is an advanced AI coding assistant that leverages Google's cutting-edge AI technology to help developers write, debug, and optimize code more efficiently. Unlike traditional code completion tools, Antigravity understands context, project structure, and best practices to provide intelligent suggestions.

## Key Features

### 1. **Intelligent Code Generation**
Antigravity can generate entire functions, classes, and even complete modules based on natural language descriptions. Simply describe what you want to build, and watch as it creates production-ready code.

### 2. **Context-Aware Suggestions**
The AI understands your entire codebase, not just the current file. It provides suggestions that align with your project's architecture, coding standards, and existing patterns.

### 3. **Advanced Debugging**
Identify and fix bugs faster with AI-powered debugging. Antigravity can analyze error messages, stack traces, and code flow to suggest precise fixes.

### 4. **Code Optimization**
Automatically optimize your code for performance, readability, and maintainability. The AI suggests refactoring opportunities and best practices.

## Real-World Applications

### Web Development
Build full-stack applications faster with intelligent component generation, API endpoint creation, and database schema design.

### Mobile Development
Create cross-platform mobile apps with optimized code for both iOS and Android platforms.

### DevOps & Infrastructure
Generate Infrastructure as Code (IaC) configurations, CI/CD pipelines, and deployment scripts with ease.

## Getting Started with Antigravity

\`\`\`javascript
// Example: Using Antigravity to generate a React component
// Prompt: "Create a responsive navbar component with dark mode toggle"

import React, { useState } from 'react';

const Navbar = () => {
  const [darkMode, setDarkMode] = useState(false);
  
  return (
    <nav className={\`navbar \${darkMode ? 'dark' : 'light'}\`}>
      <div className="nav-content">
        <h1>My App</h1>
        <button onClick={() => setDarkMode(!darkMode)}>
          {darkMode ? '☀️' : '🌙'}
        </button>
      </div>
    </nav>
  );
};

export default Navbar;
\`\`\`

## Best Practices

1. **Clear Prompts**: Provide detailed, specific descriptions of what you want to build
2. **Review Generated Code**: Always review and test AI-generated code before deploying
3. **Iterative Development**: Use Antigravity iteratively, building features step by step
4. **Security First**: Ensure generated code follows security best practices

## The Future of Development

Google Antigravity is not just a tool—it's a glimpse into the future of software development. As AI continues to evolve, we can expect even more sophisticated features that will further accelerate development workflows.

## Conclusion

Google Antigravity is transforming how we build software. By combining human creativity with AI-powered assistance, developers can focus on solving complex problems while the AI handles routine coding tasks. Whether you're a seasoned developer or just starting out, Antigravity can significantly boost your productivity.

**Ready to experience the future of coding? Start using Google Antigravity today and revolutionize your development workflow!**
    `
    },
    {
        id: "reactbits-modern-ui",
        title: "ReactBits: Building Modern UI Components",
        description: "Discover ReactBits, the ultimate library for creating beautiful, accessible, and performant React components with minimal effort.",
        date: "Nov 20, 2024",
        readTime: "10 min read",
        image: "https://images.unsplash.com/photo-1633356122544-f134324a6cee?q=80&w=2070&auto=format&fit=crop",
        category: "React",
        author: "Argha Biswas",
        content: `
# ReactBits: Building Modern UI Components

ReactBits is revolutionizing how developers create user interfaces in React applications. This comprehensive component library combines beautiful design with exceptional developer experience.

## Why ReactBits?

In the ever-evolving landscape of React development, ReactBits stands out by offering:

- **Pre-built Components**: Over 100 ready-to-use components
- **Accessibility First**: WCAG 2.1 compliant out of the box
- **Customizable**: Easy theming and styling options
- **TypeScript Support**: Full type safety
- **Performance Optimized**: Minimal bundle size

## Getting Started

\`\`\`bash
npm install reactbits
\`\`\`

\`\`\`javascript
import { Button, Card, Modal } from 'reactbits';

function App() {
  return (
    <Card>
      <h2>Welcome to ReactBits</h2>
      <Button variant="primary">Get Started</Button>
    </Card>
  );
}
\`\`\`

## Core Components

### Buttons
Create stunning buttons with various styles, sizes, and states.

### Forms
Build complex forms with validation, error handling, and accessibility features.

### Navigation
Implement responsive navigation menus, tabs, and breadcrumbs.

### Data Display
Present data beautifully with tables, cards, and lists.

## Advanced Features

### Theming System
Customize the entire look and feel of your application with a powerful theming system.

### Animation Library
Add smooth, performant animations to your components.

### Responsive Design
All components are mobile-first and fully responsive.

## Best Practices

1. Use semantic HTML elements
2. Implement proper ARIA labels
3. Test across different devices
4. Optimize for performance
5. Follow accessibility guidelines

## Conclusion

ReactBits empowers developers to build beautiful, accessible applications faster than ever. Start using ReactBits today and elevate your React development experience!
    `
    },
    {
        id: "mern-stack-2025",
        title: "The Future of MERN Stack in 2025",
        description: "Discover why the MERN stack remains the dominant choice for scalable full-stack applications and what's new in 2025.",
        date: "Nov 15, 2024",
        readTime: "15 min read",
        image: "https://images.unsplash.com/photo-1627398242454-45a1465c2479?q=80&w=2074&auto=format&fit=crop",
        category: "Full Stack",
        author: "Argha Biswas",
        content: `
# The Future of MERN Stack in 2025

The MERN stack (MongoDB, Express.js, React, Node.js) continues to dominate full-stack development in 2025. Let's explore why it remains the go-to choice for modern web applications.

## Why MERN Stack?

### 1. **JavaScript Everywhere**
Use a single language across your entire stack, from database queries to UI components.

### 2. **Scalability**
Built to handle applications of any size, from startups to enterprise solutions.

### 3. **Rich Ecosystem**
Access to millions of npm packages and a vibrant community.

### 4. **Performance**
Node.js's non-blocking I/O and React's virtual DOM ensure optimal performance.

## What's New in 2025?

### React Server Components
Reduce bundle sizes and improve initial page load times.

### MongoDB 7.0 Features
Enhanced time-series collections and improved query performance.

### Node.js 22 LTS
Better performance, enhanced security, and new APIs.

### Express 5.0
Improved routing, better error handling, and TypeScript support.

## Building a Modern MERN Application

\`\`\`javascript
// Modern MERN stack example
import express from 'express';
import mongoose from 'mongoose';
import cors from 'cors';

const app = express();

// Middleware
app.use(cors());
app.use(express.json());

// MongoDB connection
mongoose.connect(process.env.MONGODB_URI);

// API routes
app.get('/api/data', async (req, res) => {
  const data = await DataModel.find();
  res.json(data);
});

app.listen(5000, () => console.log('Server running'));
\`\`\`

## Best Practices for 2025

1. **Use TypeScript**: Add type safety to your entire stack
2. **Implement Microservices**: Scale individual components independently
3. **Optimize Performance**: Use caching, CDNs, and code splitting
4. **Security First**: Implement JWT, HTTPS, and input validation
5. **CI/CD Pipeline**: Automate testing and deployment

## Conclusion

The MERN stack's flexibility, performance, and developer experience make it the perfect choice for building modern web applications in 2025 and beyond.
    `
    },
    {
        id: "cybersecurity-web-apps",
        title: "Cybersecurity Best Practices for Web Applications",
        description: "Essential security measures every web developer should implement to protect user data and prevent common vulnerabilities.",
        date: "Nov 10, 2024",
        readTime: "14 min read",
        image: "https://images.unsplash.com/photo-1550751827-4bd374c3f58b?q=80&w=2070&auto=format&fit=crop",
        category: "Security",
        author: "Argha Biswas",
        content: `
# Cybersecurity Best Practices for Web Applications

In today's digital landscape, web application security is more critical than ever. Let's explore essential security practices every developer must implement.

## Common Web Vulnerabilities

### 1. **SQL Injection**
Attackers inject malicious SQL code to manipulate databases.

**Prevention:**
\`\`\`javascript
// Bad - Vulnerable to SQL injection
const query = \`SELECT * FROM users WHERE id = \${userId}\`;

// Good - Using parameterized queries
const query = 'SELECT * FROM users WHERE id = ?';
db.query(query, [userId]);
\`\`\`

### 2. **Cross-Site Scripting (XSS)**
Malicious scripts injected into web pages viewed by other users.

**Prevention:**
- Sanitize user input
- Use Content Security Policy (CSP)
- Escape output data

### 3. **Cross-Site Request Forgery (CSRF)**
Unauthorized commands transmitted from a trusted user.

**Prevention:**
\`\`\`javascript
// Implement CSRF tokens
app.use(csrf());
app.get('/form', (req, res) => {
  res.render('form', { csrfToken: req.csrfToken() });
});
\`\`\`

## Authentication & Authorization

### Secure Password Storage
\`\`\`javascript
import bcrypt from 'bcrypt';

// Hash password
const hashedPassword = await bcrypt.hash(password, 10);

// Verify password
const isValid = await bcrypt.compare(password, hashedPassword);
\`\`\`

### JWT Implementation
\`\`\`javascript
import jwt from 'jsonwebtoken';

// Generate token
const token = jwt.sign({ userId }, process.env.JWT_SECRET, {
  expiresIn: '24h'
});

// Verify token
const decoded = jwt.verify(token, process.env.JWT_SECRET);
\`\`\`

## HTTPS & Encryption

Always use HTTPS to encrypt data in transit:

\`\`\`javascript
import https from 'https';
import fs from 'fs';

const options = {
  key: fs.readFileSync('private-key.pem'),
  cert: fs.readFileSync('certificate.pem')
};

https.createServer(options, app).listen(443);
\`\`\`

## Security Headers

Implement essential security headers:

\`\`\`javascript
import helmet from 'helmet';

app.use(helmet());
app.use(helmet.contentSecurityPolicy({
  directives: {
    defaultSrc: ["'self'"],
    styleSrc: ["'self'", "'unsafe-inline'"]
  }
}));
\`\`\`

## Input Validation

Always validate and sanitize user input:

\`\`\`javascript
import validator from 'validator';

// Email validation
if (!validator.isEmail(email)) {
  throw new Error('Invalid email');
}

// Sanitize input
const clean = validator.escape(userInput);
\`\`\`

## Rate Limiting

Prevent brute force attacks:

\`\`\`javascript
import rateLimit from 'express-rate-limit';

const limiter = rateLimit({
  windowMs: 15 * 60 * 1000, // 15 minutes
  max: 100 // limit each IP to 100 requests per windowMs
});

app.use('/api/', limiter);
\`\`\`

## Security Checklist

- [ ] Use HTTPS everywhere
- [ ] Implement proper authentication
- [ ] Validate and sanitize all inputs
- [ ] Use security headers
- [ ] Keep dependencies updated
- [ ] Implement rate limiting
- [ ] Use CSRF protection
- [ ] Encrypt sensitive data
- [ ] Regular security audits
- [ ] Monitor for vulnerabilities

## Conclusion

Security is not a one-time task but an ongoing process. Implement these best practices, stay updated with the latest security trends, and regularly audit your applications to ensure robust protection against evolving threats.
    `
    },
    {
        id: "react-performance-optimization",
        title: "Advanced React Performance Optimization Techniques",
        description: "Master advanced techniques to boost your React application's performance and deliver exceptional user experiences.",
        date: "Nov 5, 2024",
        readTime: "13 min read",
        image: "https://images.unsplash.com/photo-1555099962-4199c345e5dd?q=80&w=2070&auto=format&fit=crop",
        category: "Performance",
        author: "Argha Biswas",
        content: `
# Advanced React Performance Optimization Techniques

Performance is crucial for user experience. Let's explore advanced techniques to make your React applications lightning-fast.

## 1. Code Splitting

Split your code into smaller chunks that load on demand:

\`\`\`javascript
import React, { lazy, Suspense } from 'react';

const HeavyComponent = lazy(() => import('./HeavyComponent'));

function App() {
  return (
    <Suspense fallback={<div>Loading...</div>}>
      <HeavyComponent />
    </Suspense>
  );
}
\`\`\`

## 2. Memoization

### React.memo
Prevent unnecessary re-renders:

\`\`\`javascript
const ExpensiveComponent = React.memo(({ data }) => {
  return <div>{/* Render data */}</div>;
});
\`\`\`

### useMemo Hook
Memoize expensive calculations:

\`\`\`javascript
const expensiveValue = useMemo(() => {
  return computeExpensiveValue(a, b);
}, [a, b]);
\`\`\`

### useCallback Hook
Memoize callback functions:

\`\`\`javascript
const handleClick = useCallback(() => {
  doSomething(a, b);
}, [a, b]);
\`\`\`

## 3. Virtual Scrolling

Handle large lists efficiently:

\`\`\`javascript
import { FixedSizeList } from 'react-window';

const Row = ({ index, style }) => (
  <div style={style}>Row {index}</div>
);

const List = () => (
  <FixedSizeList
    height={600}
    itemCount={10000}
    itemSize={35}
    width="100%"
  >
    {Row}
  </FixedSizeList>
);
\`\`\`

## 4. Image Optimization

\`\`\`javascript
import Image from 'next/image';

<Image
  src="/photo.jpg"
  alt="Photo"
  width={500}
  height={300}
  loading="lazy"
  placeholder="blur"
/>
\`\`\`

## 5. Bundle Size Optimization

### Analyze Bundle
\`\`\`bash
npm run build -- --stats
npx webpack-bundle-analyzer build/bundle-stats.json
\`\`\`

### Tree Shaking
Import only what you need:

\`\`\`javascript
// Bad
import _ from 'lodash';

// Good
import debounce from 'lodash/debounce';
\`\`\`

## 6. Debouncing & Throttling

\`\`\`javascript
import { debounce } from 'lodash';

const handleSearch = debounce((value) => {
  // API call
}, 300);
\`\`\`

## Performance Metrics

Monitor key metrics:
- First Contentful Paint (FCP)
- Largest Contentful Paint (LCP)
- Time to Interactive (TTI)
- Cumulative Layout Shift (CLS)

## Conclusion

Implementing these optimization techniques will significantly improve your React application's performance, leading to better user experience and higher engagement.
    `
    },
    {
        id: "nodejs-scalable-apis",
        title: "Building Scalable APIs with Node.js",
        description: "Learn how to design and implement RESTful APIs that can handle millions of requests with Node.js and Express.",
        date: "Oct 28, 2024",
        readTime: "16 min read",
        image: "https://images.unsplash.com/photo-1558494949-ef010cbdcc31?q=80&w=2070&auto=format&fit=crop",
        category: "Backend",
        author: "Argha Biswas",
        content: `
# Building Scalable APIs with Node.js

Building APIs that can handle millions of requests requires careful planning and implementation. Let's explore best practices for creating scalable Node.js APIs.

## API Architecture

### RESTful Design Principles

\`\`\`javascript
// Resource-based URLs
GET    /api/users          // Get all users
GET    /api/users/:id      // Get specific user
POST   /api/users          // Create user
PUT    /api/users/:id      // Update user
DELETE /api/users/:id      // Delete user
\`\`\`

## Database Optimization

### Connection Pooling

\`\`\`javascript
const pool = new Pool({
  max: 20,
  idleTimeoutMillis: 30000,
  connectionTimeoutMillis: 2000,
});
\`\`\`

### Indexing

\`\`\`javascript
// MongoDB indexing
userSchema.index({ email: 1 });
userSchema.index({ createdAt: -1 });
\`\`\`

## Caching Strategies

### Redis Implementation

\`\`\`javascript
import redis from 'redis';

const client = redis.createClient();

// Cache GET requests
app.get('/api/users/:id', async (req, res) => {
  const cached = await client.get(\`user:\${req.params.id}\`);
  
  if (cached) {
    return res.json(JSON.parse(cached));
  }
  
  const user = await User.findById(req.params.id);
  await client.setex(\`user:\${req.params.id}\`, 3600, JSON.stringify(user));
  
  res.json(user);
});
\`\`\`

## Load Balancing

### Cluster Module

\`\`\`javascript
import cluster from 'cluster';
import os from 'os';

if (cluster.isMaster) {
  const numCPUs = os.cpus().length;
  
  for (let i = 0; i < numCPUs; i++) {
    cluster.fork();
  }
} else {
  app.listen(3000);
}
\`\`\`

## Error Handling

\`\`\`javascript
// Global error handler
app.use((err, req, res, next) => {
  console.error(err.stack);
  
  res.status(err.status || 500).json({
    error: {
      message: err.message,
      status: err.status
    }
  });
});
\`\`\`

## Rate Limiting

\`\`\`javascript
import rateLimit from 'express-rate-limit';

const apiLimiter = rateLimit({
  windowMs: 15 * 60 * 1000,
  max: 100,
  message: 'Too many requests'
});

app.use('/api/', apiLimiter);
\`\`\`

## API Documentation

Use Swagger/OpenAPI:

\`\`\`javascript
import swaggerUi from 'swagger-ui-express';
import swaggerDocument from './swagger.json';

app.use('/api-docs', swaggerUi.serve, swaggerUi.setup(swaggerDocument));
\`\`\`

## Monitoring & Logging

\`\`\`javascript
import winston from 'winston';

const logger = winston.createLogger({
  level: 'info',
  format: winston.format.json(),
  transports: [
    new winston.transports.File({ filename: 'error.log', level: 'error' }),
    new winston.transports.File({ filename: 'combined.log' })
  ]
});
\`\`\`

## Conclusion

Building scalable APIs requires attention to architecture, performance, security, and monitoring. Implement these best practices to create robust APIs that can grow with your application.
    `
    },
    {
        id: "modern-css-2024",
        title: "Modern CSS Techniques for 2024",
        description: "Explore the latest CSS features and techniques to create stunning, responsive web interfaces without JavaScript.",
        date: "Oct 20, 2024",
        readTime: "11 min read",
        image: "https://images.unsplash.com/photo-1507721999472-8ed4421c4af2?q=80&w=2070&auto=format&fit=crop",
        category: "CSS & Design",
        author: "Argha Biswas",
        content: `
# Modern CSS Techniques for 2024

CSS has evolved tremendously. Let's explore modern techniques that will elevate your web designs.

## Container Queries

Responsive components based on container size:

\`\`\`css
.card-container {
  container-type: inline-size;
}

@container (min-width: 400px) {
  .card {
    display: grid;
    grid-template-columns: 1fr 2fr;
  }
}
\`\`\`

## CSS Grid Advanced Layouts

\`\`\`css
.grid {
  display: grid;
  grid-template-columns: repeat(auto-fit, minmax(300px, 1fr));
  gap: 2rem;
}
\`\`\`

## Custom Properties (CSS Variables)

\`\`\`css
:root {
  --primary-color: #9333ea;
  --spacing-unit: 8px;
  --border-radius: calc(var(--spacing-unit) * 2);
}

.button {
  background: var(--primary-color);
  padding: calc(var(--spacing-unit) * 2);
  border-radius: var(--border-radius);
}
\`\`\`

## Scroll-Driven Animations

\`\`\`css
@keyframes fade-in {
  from { opacity: 0; transform: translateY(20px); }
  to { opacity: 1; transform: translateY(0); }
}

.element {
  animation: fade-in linear;
  animation-timeline: view();
  animation-range: entry 0% cover 40%;
}
\`\`\`

## Modern Selectors

\`\`\`css
/* :has() - Parent selector */
.card:has(img) {
  display: grid;
}

/* :is() - Grouping */
:is(h1, h2, h3) {
  line-height: 1.2;
}

/* :where() - Zero specificity */
:where(ul, ol) li {
  margin-bottom: 0.5rem;
}
\`\`\`

## Clamp for Responsive Typography

\`\`\`css
h1 {
  font-size: clamp(2rem, 5vw, 4rem);
}
\`\`\`

## Aspect Ratio

\`\`\`css
.video-container {
  aspect-ratio: 16 / 9;
}
\`\`\`

## Backdrop Filter

\`\`\`css
.glass {
  background: rgba(255, 255, 255, 0.1);
  backdrop-filter: blur(10px);
}
\`\`\`

## Conclusion

Modern CSS provides powerful tools for creating beautiful, responsive designs. Embrace these techniques to build better web experiences.
    `
    },
    {
        id: "typescript-best-practices",
        title: "TypeScript Best Practices for Large-Scale Applications",
        description: "Master TypeScript patterns and practices for building maintainable, type-safe applications at scale.",
        date: "Oct 12, 2024",
        readTime: "14 min read",
        image: "https://images.unsplash.com/photo-1516116216624-53e697fedbea?q=80&w=2028&auto=format&fit=crop",
        category: "TypeScript",
        author: "Argha Biswas",
        content: `
# TypeScript Best Practices for Large-Scale Applications

TypeScript brings type safety to JavaScript. Let's explore best practices for large-scale applications.

## Type System Fundamentals

### Interface vs Type

\`\`\`typescript
// Interface - Extensible
interface User {
  id: string;
  name: string;
}

interface Admin extends User {
  role: 'admin';
}

// Type - More flexible
type ID = string | number;
type User = {
  id: ID;
  name: string;
};
\`\`\`

## Generics

\`\`\`typescript
function identity<T>(arg: T): T {
  return arg;
}

interface ApiResponse<T> {
  data: T;
  status: number;
  message: string;
}

const response: ApiResponse<User[]> = {
  data: users,
  status: 200,
  message: 'Success'
};
\`\`\`

## Utility Types

\`\`\`typescript
// Partial - Make all properties optional
type PartialUser = Partial<User>;

// Required - Make all properties required
type RequiredUser = Required<User>;

// Pick - Select specific properties
type UserPreview = Pick<User, 'id' | 'name'>;

// Omit - Exclude specific properties
type UserWithoutPassword = Omit<User, 'password'>;

// Record - Create object type
type UserRoles = Record<string, User>;
\`\`\`

## Strict Mode Configuration

\`\`\`json
{
  "compilerOptions": {
    "strict": true,
    "noImplicitAny": true,
    "strictNullChecks": true,
    "strictFunctionTypes": true,
    "noUnusedLocals": true,
    "noUnusedParameters": true
  }
}
\`\`\`

## Type Guards

\`\`\`typescript
function isUser(obj: any): obj is User {
  return 'id' in obj && 'name' in obj;
}

if (isUser(data)) {
  console.log(data.name); // TypeScript knows it's a User
}
\`\`\`

## Discriminated Unions

\`\`\`typescript
type Success = {
  type: 'success';
  data: User;
};

type Error = {
  type: 'error';
  message: string;
};

type Result = Success | Error;

function handleResult(result: Result) {
  if (result.type === 'success') {
    console.log(result.data); // TypeScript knows it's Success
  } else {
    console.log(result.message); // TypeScript knows it's Error
  }
}
\`\`\`

## Async/Await with Types

\`\`\`typescript
async function fetchUser(id: string): Promise<User> {
  const response = await fetch(\`/api/users/\${id}\`);
  const data: User = await response.json();
  return data;
}
\`\`\`

## Conclusion

TypeScript's type system provides safety and better developer experience. Follow these practices to build robust, maintainable applications.
    `
    },
    {
        id: "docker-microservices",
        title: "Containerizing Applications with Docker",
        description: "Learn how to containerize your applications with Docker for consistent deployments across all environments.",
        date: "Oct 5, 2024",
        readTime: "12 min read",
        image: "https://images.unsplash.com/photo-1605745341112-85968b19335b?q=80&w=2071&auto=format&fit=crop",
        category: "DevOps",
        author: "Argha Biswas",
        content: `
# Containerizing Applications with Docker

Docker revolutionizes application deployment. Let's explore how to containerize your applications effectively.

## Why Docker?

- **Consistency**: Same environment everywhere
- **Isolation**: Each container is isolated
- **Portability**: Run anywhere Docker runs
- **Scalability**: Easy to scale up or down

## Basic Dockerfile

\`\`\`dockerfile
FROM node:18-alpine

WORKDIR /app

COPY package*.json ./
RUN npm ci --only=production

COPY . .

EXPOSE 3000

CMD ["node", "server.js"]
\`\`\`

## Multi-Stage Builds

\`\`\`dockerfile
# Build stage
FROM node:18-alpine AS builder
WORKDIR /app
COPY package*.json ./
RUN npm ci
COPY . .
RUN npm run build

# Production stage
FROM node:18-alpine
WORKDIR /app
COPY --from=builder /app/dist ./dist
COPY package*.json ./
RUN npm ci --only=production
EXPOSE 3000
CMD ["node", "dist/server.js"]
\`\`\`

## Docker Compose

\`\`\`yaml
version: '3.8'

services:
  app:
    build: .
    ports:
      - "3000:3000"
    environment:
      - NODE_ENV=production
      - MONGODB_URI=mongodb://db:27017/myapp
    depends_on:
      - db
  
  db:
    image: mongo:6
    volumes:
      - mongo-data:/data/db
    ports:
      - "27017:27017"

volumes:
  mongo-data:
\`\`\`

## Best Practices

### 1. Use .dockerignore

\`\`\`
node_modules
npm-debug.log
.git
.env
\`\`\`

### 2. Minimize Layers

\`\`\`dockerfile
# Bad - Multiple layers
RUN npm install
RUN npm run build
RUN npm prune --production

# Good - Single layer
RUN npm install && npm run build && npm prune --production
\`\`\`

### 3. Use Specific Tags

\`\`\`dockerfile
# Bad
FROM node:latest

# Good
FROM node:18.17.0-alpine
\`\`\`

## Container Orchestration

### Docker Swarm

\`\`\`bash
docker swarm init
docker service create --name web --replicas 3 -p 80:80 myapp
\`\`\`

### Kubernetes

\`\`\`yaml
apiVersion: apps/v1
kind: Deployment
metadata:
  name: myapp
spec:
  replicas: 3
  selector:
    matchLabels:
      app: myapp
  template:
    metadata:
      labels:
        app: myapp
    spec:
      containers:
      - name: myapp
        image: myapp:latest
        ports:
        - containerPort: 3000
\`\`\`

## Monitoring & Logging

\`\`\`bash
# View logs
docker logs -f container_name

# Monitor resources
docker stats

# Inspect container
docker inspect container_name
\`\`\`

## Conclusion

Docker simplifies deployment and ensures consistency across environments. Master these concepts to streamline your development workflow.
    `
    },
    {
        id: "graphql-rest-comparison",
        title: "GraphQL vs REST: Choosing the Right API Architecture",
        description: "Compare GraphQL and REST APIs to make informed decisions about your application's data fetching strategy.",
        date: "Sep 28, 2024",
        readTime: "10 min read",
        image: "https://images.unsplash.com/photo-1555949963-ff9fe0c870eb?q=80&w=2070&auto=format&fit=crop",
        category: "API Design",
        author: "Argha Biswas",
        content: `
# GraphQL vs REST: Choosing the Right API Architecture

Choosing between GraphQL and REST is crucial for your application's success. Let's compare both approaches.

## REST API

### Advantages
- Simple and well-understood
- Great caching support
- Multiple endpoints for different resources
- Stateless architecture

### Example

\`\`\`javascript
// Multiple endpoints
GET /api/users/1
GET /api/users/1/posts
GET /api/users/1/comments
\`\`\`

## GraphQL

### Advantages
- Single endpoint
- Request exactly what you need
- No over-fetching or under-fetching
- Strong typing
- Real-time subscriptions

### Example

\`\`\`graphql
query {
  user(id: 1) {
    name
    email
    posts {
      title
      comments {
        text
        author
      }
    }
  }
}
\`\`\`

## Implementation

### REST with Express

\`\`\`javascript
app.get('/api/users/:id', async (req, res) => {
  const user = await User.findById(req.params.id);
  res.json(user);
});
\`\`\`

### GraphQL with Apollo Server

\`\`\`javascript
const typeDefs = gql\`
  type User {
    id: ID!
    name: String!
    email: String!
    posts: [Post!]!
  }

  type Query {
    user(id: ID!): User
  }
\`;

const resolvers = {
  Query: {
    user: (_, { id }) => User.findById(id)
  },
  User: {
    posts: (user) => Post.find({ userId: user.id })
  }
};
\`\`\`

## When to Use REST

- Simple CRUD operations
- Caching is critical
- Public APIs
- File uploads/downloads

## When to Use GraphQL

- Complex data requirements
- Mobile applications
- Microservices architecture
- Real-time features

## Performance Comparison

### REST
- Multiple round trips
- Over-fetching data
- Simple caching

### GraphQL
- Single request
- Exact data needed
- Complex caching

## Conclusion

Both REST and GraphQL have their place. Choose based on your specific requirements, team expertise, and application needs.
    `
    }
];

export const getBlogById = (id) => {
    return blogPosts.find(post => post.id === id);
};

export const getRecentBlogs = (count = 3) => {
    return blogPosts.slice(0, count);
};
